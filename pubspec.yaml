name: flutter_20mob_project_final
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path:
  sqflite: ^1.3.2+3
  floor: ^0.19.0
  cupertino_icons: ^1.0.2
  cached_network_image: 2.1.0+1
  dio: ^3.0.8
  rxdart: ^0.23.1
  flare_flutter: ^1.5.9
  auto_size_text: ^2.1.0
  animated_text_kit: ^1.3.1
  firebase_core: ^0.4.0
  cloud_firestore: ^0.13.0+1
  google_sign_in: ^4.1.1
  firebase_auth: ^0.15.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  floor_generator: ^0.19.0
  build_runner: ^1.10.0

flutter:
  uses-material-design: true

  assets:
   - images/
   - assets/